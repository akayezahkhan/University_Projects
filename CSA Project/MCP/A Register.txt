module Areg(
	 input clk,
	 input rst,
    input [31:0] in,
    output [31:0] A_out
    );

	reg [31:0] A;
  
  always@(posedge clk or posedge rst)
    begin
      if (rst == 1)	
        A <= 32'b x;
      else
        A <= in;
	 end
	 
	assign A_out = A;
	
endmodule